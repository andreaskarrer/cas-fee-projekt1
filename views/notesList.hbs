{{#each this}}
    {{#if _id}}
    <li>
        <div class="row">
            <div class="due datefield">{{due}}</div>
            <div class="title">{{title}}</div>
            {{#fivesymbols importance}}⚡{{/fivesymbols}}
            <a href="/note/{{ _id }}"><button>Edit</button></a>
        </div>
        <div class="row">
            <div class="datefield">
                <div class="created">{{created}}</div>
                <div><input type="checkbox" class="done" disabled {{#if finished}}checked{{/if}}> Done</div>
            </div>
            <div class="desc">{{desc}}</div>
        </div>
    </li>
    {{/if}}
{{/each}}
